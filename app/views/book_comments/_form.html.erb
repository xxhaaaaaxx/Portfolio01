<!-- コメントフォーム(部分テンプレ) -->

<%= form_with model: [book, book_comment] do |f| %>
  <%= f.text_area :comment, size: "60x3", placeholder: "コメントする" %>
  <%= f.submit "送信" %>
<% end %>

<!-- form_withに対して、[book, book_comment]のように、配列で2つの変数を指定
特定の本に対してコメントを投稿する場合、そのコメントはその本と関連付けられる
この場合、コメント投稿フォームはコメントの内容だけでなく、
どの本に対するコメントなのかもフォームで指定する必要がある-->

<!-- rows:'x'：テキストエリアをx行に表示
placeholder: テキストエリア内に表示されるテキストを指定
class: "w-100"：Bootstrapのクラスで、テキストエリアの幅を100%（width: 100%）に設定-->